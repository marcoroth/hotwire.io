---
title: Getting started with Hotwire in Amber
framework: Amber
language: Crystal
description: A framework that implements the MVC pattern using the object-oriented paradigm with Crystal.
icon_class: devicon-crystal-original text-6xl
---

<%= render Page::ContainerComponent.new(page: current_page) do |page| %>
  <% page.with_title(title: current_page.data.fetch("title")) %>

  <%= render Page::ContributeComponent.new(file: current_page.asset.path.path) %>
<% end %>
