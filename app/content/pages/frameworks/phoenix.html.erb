---
title: Getting started with Hotwire in Phoenix
framework: Phoenix
language: Elixir
description: A framework to build rich and interactive web applications with Elixir.
icon_class: devicon-phoenix-plain-wordmark colored text-6xl
---

<%= render Page::ContainerComponent.new(page: current_page) do |page| %>
  <% page.with_title(title: current_page.data.fetch("title")) %>

  <%= render Page::ContributeComponent.new(file: current_page.asset.path.path) %>
<% end %>
