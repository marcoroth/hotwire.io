---
title: Channels publishing Hotwire content
---

<%= render UI::ContainerComponent.new do %>
  <%= render UI::TitleComponent.new(title: current_page.data.fetch("title")) %>

  <% Channel.order(name: :asc).each do |channel| %>
    <%= link_to channel.name, channel.url, target: :blank %><br/>
  <% end %>
<% end %>
